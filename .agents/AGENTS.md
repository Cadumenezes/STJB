# Custom Rules for Danceflow

- **Git Push Constraint**: Nunca execute o comando `git push` ou envie commits para repositórios remotos por conta própria. Apenas faça push quando for explicitamente instruído ou comandado pelo usuário.
